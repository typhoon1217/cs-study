# 프로세스 동기화 및 병행 제어 심층 학습 가이드

## 1. 기본 개념 이해

### 1.1 프로세스와 스레드

#### 프로세스

- 메모리에 로드된 프로그램의 동적 인스턴스
- 독립적인 실행 단위
- 고유한 메모리 공간 보유 (코드, 데이터, 스택, 힙)

#### 스레드

- 프로세스 내부의 실행 단위
- 프로세스 자원 공유 (메모리 공간)
- 경량화된 실행 흐름

### 1.2 동시성 vs 병렬성

#### 동시성 (Concurrency)

- 여러 작업이 동시에 실행되는 것처럼 보이는 상태
- 시분할 시스템에서 여러 작업 번갈아 수행
- 단일 코어에서도 가능

#### 병렬성 (Parallelism)

- 실제로 동시에 여러 작업 수행
- 멀티 코어 시스템에서 가능
- 동시에 독립적인 작업 실행

## 2. 동기화의 필요성

### 2.1 경쟁 조건 (Race Condition)

- 둘 이상의 스레드가 공유 자원에 동시 접근
- 실행 순서에 따라 결과 달라짐
- 예측 불가능한 결과 발생

#### 경쟁 조건 예시

```java
public class Counter {
    private int count = 0;

    public void increment() {
        // 안전하지 않은 증가 연산
        count++; // 원자적 연산 아님
    }
}
```

### 2.2 임계 영역 (Critical Section)

- 공유 자원에 접근하는 코드 영역
- 한 번에 하나의 스레드만 접근 가능
- 상호 배제 필요

## 3. 동기화 메커니즘

### 3.1 뮤텍스 (Mutex)

#### 개념

- 상호 배제를 위한 동기화 기법
- 오직 한 스레드만 접근 허용
- 잠금(Lock) 메커니즘

#### 구현 예시

```java
public class MutexExample {
    private static final Object mutex = new Object();
    private static int sharedValue = 0;

    public void criticalSection() {
        synchronized(mutex) {
            // 오직 한 스레드만 접근 가능
            sharedValue++;
        }
    }
}
```

### 3.2 세마포어 (Semaphore)

#### 개념

- 제한된 수의 스레드 동시 접근 허용
- 카운팅 세마포어와 이진 세마포어

#### 구현 예시

```java
import java.util.concurrent.Semaphore;

public class SemaphoreExample {
    // 최대 3개 스레드 동시 접근
    private static Semaphore semaphore = new Semaphore(3);

    public void limitedAccess() {
        try {
            semaphore.acquire(); // 접근 허가 요청
            // 제한된 자원 사용
        } catch (InterruptedException e) {
            e.printStackTrace();
        } finally {
            semaphore.release(); // 자원 해제
        }
    }
}
```

### 3.3 조건 변수 (Condition Variable)

#### 개념

- 스레드 간 통신 메커니즘
- 특정 조건 만족 시까지 스레드 대기

#### 구현 예시

```java
import java.util.concurrent.locks.Condition;
import java.util.concurrent.locks.Lock;
import java.util.concurrent.locks.ReentrantLock;

public class BufferExample {
    private final Lock lock = new ReentrantLock();
    private final Condition notFull = lock.newCondition();
    private final Condition notEmpty = lock.newCondition();
    private final int[] buffer = new int[100];
    private int count = 0;

    public void produce(int item) throws InterruptedException {
        lock.lock();
        try {
            while (count == buffer.length) {
                notFull.await(); // 버퍼 가득 찼을 때 대기
            }
            buffer[count++] = item;
            notEmpty.signal(); // 소비자에게 신호
        } finally {
            lock.unlock();
        }
    }

    public int consume() throws InterruptedException {
        lock.lock();
        try {
            while (count == 0) {
                notEmpty.await(); // 버퍼 비었을 때 대기
            }
            int item = buffer[--count];
            notFull.signal(); // 생산자에게 신호
            return item;
        } finally {
            lock.unlock();
        }
    }
}
```

### 3.4 모니터 (Monitor)

#### 개념

- 상호 배제와 조건 동기화 통합
- 객체 수준의 동기화 메커니즘
- synchronized 키워드로 구현

#### 구현 예시

```java
public class BoundedBuffer {
    private int[] buffer;
    private int count = 0;
    private int capacity;

    public BoundedBuffer(int capacity) {
        this.capacity = capacity;
        buffer = new int[capacity];
    }

    public synchronized void put(int item) throws InterruptedException {
        while (count == capacity) {
            wait(); // 버퍼 가득 찼을 때 대기
        }
        buffer[count++] = item;
        notifyAll(); // 대기 중인 스레드에 신호
    }

    public synchronized int get() throws InterruptedException {
        while (count == 0) {
            wait(); // 버퍼 비었을 때 대기
        }
        int item = buffer[--count];
        notifyAll(); // 대기 중인 스레드에 신호
        return item;
    }
}
```

## 4. 교착상태 (Deadlock)

### 4.1 교착상태 발생 조건 (4가지)

1. 상호 배제 (Mutual Exclusion)
2. 점유 대기 (Hold and Wait)
3. 비선점 (No Preemption)
4. 순환 대기 (Circular Wait)

### 4.2 교착상태 해결 전략

#### 예방 (Prevention)

- 4가지 발생 조건 중 최소 하나 제거
- 자원 할당 순서 제한
- 타임아웃 메커니즘 도입

#### 회피 (Avoidance)

- 은행원 알고리즘 사용
- 자원 요청 전 안전성 검사

#### 탐지 및 복구 (Detection and Recovery)

- 주기적 교착상태 탐지
- 자원 선점 및 롤백

#### 예시 코드

```java
public class DeadlockPreventionExample {
    private static final Object RESOURCE_A = new Object();
    private static final Object RESOURCE_B = new Object();

    public void preventDeadlock() {
        // 자원 획득 순서 일관성 유지
        synchronized (RESOURCE_A) {
            // RESOURCE_A 작업
            synchronized (RESOURCE_B) {
                // RESOURCE_B 작업
            }
        }
    }

    // 타임아웃을 통한 교착상태 방지
    public void timeoutBasedPrevention() {
        try {
            // tryLock() 메서드로 대기 시간 제한
            if (lock.tryLock(1, TimeUnit.SECONDS)) {
                try {
                    // 임계 영역 작업
                } finally {
                    lock.unlock();
                }
            }
        } catch (InterruptedException e) {
            // 예외 처리
        }
    }
}
```

## 5. 추가 고려사항

### 5.1 성능 최적화

- 최소한의 동기화 영역
- 가벼운 락킹 메커니즘
- 불필요한 동기화 최소화

### 5.2 추천 동기화 도구

- java.util.concurrent 패키지
- ReentrantLock
- AtomicInteger
- ConcurrentHashMap

## 결론

동기화는 복잡하지만 필수적인 개념입니다. 각 메커니즘의 장단점을 이해하고 상황에 맞게 선택하는 것이 중요합니다.



## 질문

1. Q: 프로세스와 스레드의 차이점은 무엇인가요? A: 프로세스는 독립된 메모리 공간을 가진 실행 프로그램의 인스턴스입니다. 반면에 스레드는 프로세스 내부에서 실행되는 최소 실행 단위로, 같은 프로세스의 스레드들은 메모리 공간을 공유합니다. 쉽게 말해, 프로세스는 독립된 집, 스레드는 그 집 안의 방들이라고 생각하시면 됩니다.
    
2. Q: 동기화가 필요한 이유는 무엇인가요? A: 동기화는 여러 스레드가 동시에 공유 자원에 접근할 때 발생할 수 있는 데이터 불일치 문제를 해결하기 위해 필요합니다. 예를 들어, 은행 계좌의 잔고를 동시에 여러 스레드가 변경한다면 예상치 못한 결과가 발생할 수 있죠. 동기화를 통해 이러한 문제를 방지하고 데이터의 일관성을 유지할 수 있습니다.
    
3. Q: 뮤텍스(Mutex)와 세마포어(Semaphore)의 차이점은 무엇인가요? A: 뮤텍스는 오직 한 스레드만 임계 영역에 들어갈 수 있는 배타적 잠금 메커니즘입니다. 세마포어는 지정된 수만큼의 스레드가 동시에 자원에 접근할 수 있도록 허용합니다. 뮤텍스는 '한 명만 들어올 수 있는 독점 회의실'과 같고, 세마포어는 '최대 3명까지 허용되는 회의실'과 비슷하다고 생각하시면 됩니다.
    
4. Q: 교착상태(Deadlock)란 무엇이고, 어떻게 해결할 수 있나요? A: 교착상태는 두 개 이상의 스레드가 서로 자원을 점유한 채 다른 자원을 무한히 기다리는 상황입니다. 네 명의 사람이 서로의 젓가락을 기다리며 식사를 못하는 식당 상황과 비슷합니다. 해결 방법으로는 예방(자원 할당 순서 제한), 회피(자원 요청 전 안전성 검사), 탐지 및 복구(주기적 상태 확인 및 강제 종료) 등이 있습니다.
    
5. Q: 조건 변수(Condition Variable)의 주요 사용 목적은 무엇인가요? A: 조건 변수는 스레드 간 통신을 위해 사용됩니다. 특정 조건이 충족될 때까지 스레드를 대기시키고, 조건이 만족되면 대기 중인 스레드에 신호를 보내 작업을 계속할 수 있게 합니다. 생산자-소비자 문제에서 버퍼가 가득 찼을 때 생산자를 대기시키고, 비어있을 때 소비자를 대기시키는 것이 대표적인 예입니다.
    
6. Q: 동시성(Concurrency)과 병렬성(Parallelism)의 차이점은 무엇인가요? A: 동시성은 여러 작업이 동시에 실행되는 것처럼 보이는 상태로, 주로 시분할 시스템에서 여러 작업을 번갈아 수행합니다. 병렬성은 실제로 동시에 여러 작업을 수행하는 것으로, 멀티코어 시스템에서 가능합니다. 한 명의 요리사가 여러 요리를 번갈아 만드는 것이 동시성이고, 여러 요리사가 동시에 요리를 만드는 것이 병렬성이라고 생각하시면 됩니다.
    
7. Q: 상호 배제(Mutual Exclusion)에 대해 설명해주세요. A: 상호 배제는 동시에 오직 한 스레드만 특정 자원이나 임계 영역에 접근할 수 있도록 보장하는 동기화 메커니즘입니다. 화장실에 한 명만 들어갈 수 있는 것처럼, 공유 자원에 대해 한 번에 한 스레드만 접근할 수 있도록 합니다. synchronized 키워드나 뮤텍스가 대표적인 상호 배제 방법입니다.
    

이러한 질문들은 프로세스 동기화의 핵심 개념을 이해하고 있는지 확인하는 데 도움이 될 것입니다. 면접에서는 개념뿐만 아니라 실제 코드나 구체적인 예시를 들어 설명할 수 있는 능력도 중요합니다.