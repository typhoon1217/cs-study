# 파일시스템과 디렉터리 스터디 노트

## 파일과 디렉터리의 기본 개념

### 파일의 특성

파일은 컴퓨터에서 데이터를 저장하는 기본 단위. 파일에는 다양한 속성과 메타데이터가 포함됨:

- **파일 속성**: 이름, 유형, 크기, 권한, 소유자 정보
- **메타데이터**: 생성 날짜, 마지막 접근/수정 날짜, 위치 정보
- **접근 단위**: 파일은 블록 단위로 접근하며, 개별 섹터 단위로 접근하지 않음

**리눅스에서 파일 정보 확인**: `$ file filename` 명령어로 파일 유형 확인 가능

### 디렉터리(폴더)

디렉터리는 파일들을 구조적으로 관리하는 특별한 유형의 파일:

- 계층적인 구조로 파일을 체계적으로 관리 가능
- 리눅스에서는 "모든 것이 파일"이라는 철학에 따라 디렉터리도 파일의 일종으로 취급

## 파티션과 파일시스템

### 파티션(Partition)

파티션은 물리적 저장 장치를 논리적으로 분할한 영역:

- **파티셔닝**: 하드 디스크와 같은 보조 기억장치를 여러 구획으로 나누는 작업
- **목적**: 데이터 관리, 다중 운영체제 설치, 시스템 복구 용이성 확보

**리눅스에서 파티션 정보 확인**:

```
$ df -h     # 현재 마운트된 파티션과 용량 표시
$ sudo fdisk -l   # 시스템의 모든 파티션 정보 표시
```

### 파일시스템(File System)

파일시스템은 파일과 디렉터리를 저장하고 관리하는 방법과 구조:

- **포매팅**: 파티션에 특정 파일시스템을 생성하는 작업
- **마운트(mount)**: 파일시스템을 접근 가능한 특정 디렉터리에 연결하는 작업
- 리눅스에서는 각 파티션에 서로 다른 파일시스템 사용 가능

### fdisk 주요 기능

`fdisk`는 리눅스에서 파티션 관리를 위한 명령줄 도구:

- 새 파티션 생성 및 삭제
- 파티션 크기 조정
- 파티션 유형 변경
- 부팅 가능 파티션 설정

**사용 예**:

```
$ man fdisk    # fdisk 매뉴얼 보기
$ sudo fdisk /dev/sda   # sda 디스크 파티션 관리
```

## 파일시스템 종류와 특성

### 주요 파일시스템 유형

1. **FAT 기반 파일시스템**
    
    - FAT(File Allocation Table): 간단한 구조, 호환성 좋음
    - FAT32, exFAT: USB 드라이브, 메모리 카드에 널리 사용
    - 블록 단위로 파일 접근
2. **아이노드 기반 파일시스템**
    
    - UNIX/Linux 계열 파일시스템(ext, XFS 등)
    - 아이노드: 파일의 메타데이터를 저장하는 색인 블록
    - 파일 데이터와 메타데이터를 분리하여 관리
3. **저널링 파일시스템**
    
    - 변경 사항을 먼저 저널(로그)에 기록한 후 실제 파일시스템에 적용
    - 시스템 충돌 시 빠른 복구 가능
    - ext3, ext4, NTFS, XFS, BTRFS 등이 저널링 지원

### 주요 파일시스템 비교

|파일시스템|특징|주로 사용되는 OS|
|---|---|---|
|FAT32|단순 구조, 호환성 높음, 4GB 파일 크기 제한|범용, 이동식 매체|
|NTFS|저널링, 권한 관리, 대용량 지원|Windows|
|ext4|저널링, 대용량 지원, 성능 최적화|Linux|
|XFS|고성능, 대용량 파일시스템|Linux 서버 환경|
|APFS|저널링, SSD 최적화, 스냅샷|macOS|
|BTRFS|저널링, 스냅샷, 볼륨 관리|Linux|

## 아이노드와 디렉터리 구조

### 아이노드 시스템

아이노드는 Unix/Linux 파일시스템의 핵심 구성요소:

- 각 파일/디렉터리는 고유한 아이노드 번호를 가짐
- 아이노드에 저장되는 정보:
    - 파일 크기, 소유자/그룹, 접근 권한
    - 생성/수정/접근 시간 등 타임스탬프
    - 데이터 블록 포인터 (직접/간접 포인터)

**리눅스에서 아이노드 확인**:

```
$ ls -i filename   # 파일의 아이노드 번호 확인
$ stat filename    # 파일의 상세 정보와 아이노드 정보 확인
```

### 디렉터리 구조

리눅스/Unix 디렉터리는 파일명과 아이노드 번호를 연결하는 테이블:

- 디렉터리는 "파일명 → 아이노드 번호" 매핑을 저장
- 하드링크: 같은 아이노드를 가리키는 여러 파일명
- 심볼릭링크: 다른 파일/디렉터리 경로를 가리키는 특수 파일

## 저널링 파일시스템 심화

### 저널링 작동 방식

1. **변경 예정 작업을 저널(로그)에 기록**
2. **저널에 기록된 작업 수행**
3. **작업 완료 후 저널 항목 제거**

### 저널링 수준

1. **메타데이터 저널링**: 파일 메타데이터만 저널에 기록 (성능 중시)
2. **전체 저널링**: 메타데이터와 파일 데이터 모두 저널에 기록 (안정성 중시)

### 장점

- 시스템 충돌 후 빠른 복구 (fsck 시간 단축)
- 데이터 일관성 향상
- 파일시스템 손상 위험 감소

## 리눅스에서 디바이스 관리

리눅스에서는 `/dev` 디렉터리를 통해 모든 장치에 접근:

- **블록 디바이스**: 하드 디스크, SSD 등 (예: `/dev/sda`)
- **문자 디바이스**: 키보드, 마우스 등 (예: `/dev/tty`)
- **가상 디바이스**: `/dev/null`, `/dev/random` 등

**디바이스 관리 명령어**:

```
$ lsblk                # 블록 디바이스 목록 표시
$ sudo blkid           # 블록 디바이스 UUID 및 파일시스템 표시
$ ls -la /dev          # 모든 디바이스 파일 표시
```

## 실제 예시와 비유

### 파일시스템 작동 예시

**파일 생성과 저장 과정**:

1. 새 파일 생성 요청 → 파일시스템이 새 아이노드 할당
2. 파일 데이터 저장을 위한 데이터 블록 할당
3. 디렉터리 항목에 파일명과 아이노드 번호 추가
4. 아이노드에 파일 메타데이터와 데이터 블록 포인터 기록

**비유**: 파일시스템은 도서관과 같음. 아이노드는 도서 카탈로그 카드, 데이터 블록은 책의 페이지, 디렉터리는 책의 목차와 같은 역할.

### 파티션과 마운트 예시

USB 드라이브 연결 및 사용 과정:

1. USB 연결 → 리눅스가 `/dev/sdb`와 같은 디바이스로 인식
2. 파티션 확인: `sudo fdisk -l /dev/sdb`
3. 마운트 포인트 생성: `sudo mkdir /mnt/usb`
4. 마운트: `sudo mount /dev/sdb1 /mnt/usb`
5. 사용 후 언마운트: `sudo umount /mnt/usb`

**비유**: 마운트는 외부 도로(디바이스)를 도시 내부 도로(파일시스템)에 연결하는 것과 같음.

## 파일시스템 관련 면접 질문 및 답변

스터티 질문 선택:  3, 5, 6, 7

### 질문 1: 파일시스템이란 무엇이며, 어떤 역할을 하나요?

**답변**: 파일시스템은 저장 장치에서 파일을 저장, 관리, 접근하는 방법을 정의하는 구조와 규칙입니다. 주요 역할로는:

- 데이터 저장 공간 효율적 관리
- 파일 메타데이터(이름, 권한, 생성일 등) 관리
- 파일 데이터 저장 위치 추적
- 파일 액세스 제어 및 보안 관리
- 파일 찾기와 읽기/쓰기 작업 지원

### 질문 2: 저널링 파일시스템의 장점은 무엇인가요?

**답변**: 저널링 파일시스템의 주요 장점은:

- 시스템 충돌 후 빠른 복구 가능 (전체 파일시스템 검사 불필요)
- 데이터 일관성 및 무결성 향상
- 파일시스템 손상 위험 감소
- 예상치 못한 전원 손실 시에도 데이터 보호 향상
- 특히 대용량 파일시스템에서 복구 시간 대폭 단축

### 질문 3: 아이노드는 무엇이며 파일시스템에서 어떤 역할을 하나요?

**답변**: 아이노드는 Unix/Linux 파일시스템에서 파일의 메타데이터를 저장하는 데이터 구조입니다. 각 파일은 고유한 아이노드 번호를 가지며, 아이노드는:

- 파일 크기, 소유자, 그룹, 접근 권한 저장
- 파일 생성/수정/접근 시간 기록
- 실제 데이터가 저장된 디스크 블록의 위치 정보(포인터) 저장
- 파일 유형(일반 파일, 디렉토리, 링크 등) 표시 아이노드는 파일 데이터와 메타데이터를 분리함으로써 파일시스템 관리 효율성을 높입니다.

### 질문 4: 하드링크와 심볼릭링크의 차이는 무엇인가요?

**답변**:

- **하드링크**: 동일한 아이노드를 가리키는 여러 파일명. 원본 파일과 완전히 동일한 파일이며 같은 데이터를 공유합니다. 원본이 삭제되어도 하드링크는 계속 작동합니다. 다른 파일시스템에는 생성할 수 없고, 디렉토리에 대한 하드링크는 생성할 수 없습니다.
- **심볼릭링크(소프트링크)**: 다른 파일이나 디렉토리의 경로를 가리키는 특수 파일. 원본 파일이 삭제되면 링크는 깨집니다. 다른 파일시스템에도 생성 가능하고 디렉토리에 대한 링크도 생성할 수 있습니다.

### 질문 5: 파티션과 포매팅의 차이는 무엇인가요?

**답변**:

- **파티션**: 물리적 저장 장치를 논리적으로 분할하는 작업. 하나의 디스크를 여러 독립적인 영역으로 나누는 것으로, 각 파티션은 별개의 디스크처럼 취급됩니다.
- **포매팅**: 파티션에 특정 파일시스템을 생성하는 작업. 파티션에 데이터를 저장하고 관리하기 위한 구조를 만드는 과정으로, 기존 데이터를 모두 지웁니다.

간단히 말해, 파티션은 디스크를 여러 구획으로 나누는 것이고, 포매팅은 그 구획에 파일을 저장할 수 있는 시스템을 설치하는 것입니다.

### 질문 6: 마운트란 무엇이며 왜 필요한가요?

**답변**: 마운트는 파일시스템을 특정 디렉토리에 연결하여 접근 가능하게 만드는 작업입니다. 리눅스/Unix 시스템에서 모든 파일시스템은 단일 디렉토리 트리의 일부로 통합되어야 사용할 수 있기 때문에 필요합니다. 예를 들어, USB 드라이브를 연결했을 때 `/mnt/usb`와 같은 디렉토리에 마운트해야 내용에 접근할 수 있습니다. 마운트는 물리적으로 분리된 저장 장치들을 하나의 통합된 파일 트리로 사용할 수 있게 해주는 필수적인 개념입니다.

### 질문 7: 리눅스에서 `/dev` 디렉토리의 역할은 무엇인가요?

**답변**: `/dev` 디렉토리는 리눅스에서 시스템의 모든 하드웨어 장치와 가상 장치를 파일로 표현하여 저장하는 곳입니다. "모든 것이 파일"이라는 유닉스 철학에 따라, 하드 디스크(`/dev/sda`), 키보드(`/dev/tty`), 랜덤 숫자 생성기(`/dev/random`) 등의 모든 장치가 파일처럼 접근됩니다. 응용 프로그램은 일반 파일과 동일한 방식으로 이 장치 파일에 읽기/쓰기 작업을 수행하여 하드웨어와 상호작용할 수 있습니다.

### 질문 8: 파일시스템 선택 시 고려해야 할 주요 요소는 무엇인가요?

**답변**: 파일시스템 선택 시 고려해야 할 주요 요소는:

- **호환성**: 사용 중인 운영체제와의 호환성
- **안정성**: 파일 손상으로부터의 보호 및 복구 기능
- **성능**: 읽기/쓰기 속도, 대용량 파일 처리 능력
- **확장성**: 대용량 저장장치 지원 여부
- **특수 기능**: 저널링, 압축, 암호화, 스냅샷 등의 지원
- **사용 목적**: 서버용, 일반 사용자용, 이동식 매체용 등
- **하드웨어 특성**: SSD와 HDD에 최적화된 파일시스템이 다를 수 있음

### 질문 9: BTRFS의 주요 특징과 장점은 무엇인가요?

**답변**: BTRFS(B-tree File System, "Butter FS"라고도 함)는 현대적인 리눅스 파일시스템으로 다음과 같은 특징을 가집니다:

- **Copy-on-Write(CoW) 기능**: 데이터 쓰기 시 원본 데이터 보존
- **스냅샷**: 파일시스템 상태를 빠르게 저장하고 복원 가능
- **내장된 RAID 기능**: 소프트웨어적으로 디스크 미러링/스트라이핑 지원
- **자가 복구**: 데이터 오류를 자동으로 감지하고 수정
- **온라인 볼륨 관리**: 실행 중 볼륨 크기 조정 가능
- **하위 볼륨**: 하나의 파티션 내에서 여러 독립적인 파일시스템 공간 생성
- **투명한 압축**: 디스크 공간 절약을 위한 데이터 압축 지원

BTRFS는 데이터 무결성과 관리 유연성을 중시하는 서버 환경이나 고급 사용자 시스템에 적합합니다.

## 요약

파일시스템은 컴퓨터가 데이터를 저장하고 관리하는 방법을 정의하는 핵심 구성요소. 다양한 파일시스템이 존재하며, 각각 고유한 특성과 장단점을 가집니다. 리눅스는 아이노드 기반 파일시스템을 주로 사용하며, 저널링 기능을 통해 시스템 안정성을 높입니다. 파티션과 마운트를 통해 물리적 저장 장치를 효율적으로 관리하며, 다양한 명령어를 통해 파일시스템을 관리할 수 있습니다.